# main.py
from prepare_data import prepare_dataset
from train_model import configure_accelerate, train_model
from inference import run_inference

if __name__ == "__main__":
    
    # prepare_dataset()
    # configure_accelerate()
    # train_model()
    run_inference()
